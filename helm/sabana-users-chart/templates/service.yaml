apiVersion: v1
kind: Service
metadata:
  name: {{ include "sabana-users-chart.fullname" . }}
  labels:
    {{- include "sabana-users-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sabana-users-chart.selectorLabels" . | nindent 4 }}
